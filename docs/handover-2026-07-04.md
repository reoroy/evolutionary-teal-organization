# ETO Handover — 2026-07-04 (1/1 场)

> 生成日期: 2026-07-04
> 上一场: [handover-2026-06-23](handover-2026-06-23.md)（估计，此前已多次 compact）

---

## 一、本场做了什么

### v0.5.0 发布 — 智子 v2 完整实现

| 做成 | 踩坑 |
|:-----|:------|
| 智子 v2 新接口 + 四种规则类型 (block/confirm/transform/track_turns) | 无 — 按 plan 直写 |
| 逃生门防循环（用户消息重置计数器） | 审计发现 setTimeout 非用户消息重置 → 已修正 |
| 强化 confirm + 预览命令（rm→ls, dd→lsblk） | — |
| trigger glob 匹配 (bash/write/edit/git:*) | — |
| 向后兼容旧配置格式 | — |
| transform / track_turns 挂接到 before_agent_start | 审计发现未挂接 → 已补充 |
| 17/17 测试通过 | — |
| v0.5.0 tag + push | — |

### 引导 + 记忆系统

| 做成 | 说明 |
|:-----|:------|
| `bootstrap/seed_guide.py` | 安装时将 8 节使用指南写入 `~/.eto/memory/guide.json` |
| `docs/usage.md` | 同上内容，仓库跟踪版本 |
| `guide.json` source + tags 字段 | 每节标注来源文档和检索标签 |

### README 更新

- 智子 v2 完整教程替换旧节
- 新增 dev slash 命令表、`docs/usage.md` 引用

### ETO Mesh 规划

| 文档 | 内容 |
|:-----|:------|
| `plan-eto-mesh.md` | 四阶段跨 Agent 协作网络架构 |
| 参考缝合对照表 | ProtoLink, raft-lite, VotingAI, Maestro 等可缝项目 |

---

## 二、P0 未完成（优先级排序）

| # | 事项 | 当前状态 | 建议下一步 |
|:-:|:-----|:---------|:-----------|
| 1 | Agent Registry + 心跳 (P1) | Plan 已写，未实现 | 实现 `~/.eto/registry.json` + `/agents` 命令 |
| 2 | 跨平台安装 (P2) | Plan 已写，未实现 | Claude Code / Reasonix / Hermes 安装脚本 |
| 3 | dispatch_spec (P3) | Plan 已写，未实现 | 可指定 target/prompt/skill/tool |
| 4 | 分布式共享记忆 (P4) | Plan 已写，未实现 | MCP 广播同步 |

---

## 三、死胡同（从台账引用已验证不行的方案）

无新增死胡同。参见 [`docs/dead-ends.md`](dead-ends.md)：

- ❌ 不要写 TUI — Pi CLI 自带 pi-tui
- ❌ 不要封装 CLI 调用 — Pi 就是 CLI
- ❌ 不要自建上下文管理 — Pi 的 JSONL 会话树
- ❌ 不要自建记忆系统 — 用 seed_guide + shared_memory
- ❌ `require_tools` 方案 — Agent 可表面调用绕过，改用 preview+confirm

---

## 四、活跃分支 / 修改文件

**当前 main，无分支。** 本场改动的文件：

```
eto/extensions/eto.ts        — 智子 v2 核心实现（~740 行）
bootstrap/__init__.py         — 编排中调用 seed_guide
bootstrap/seed_guide.py       — 新增：写入 guide.json（含 source+tags）
bootstrap/seed_sample_skills.py — 未改
docs/usage.md                 — 新增：仓库版使用指南
docs/handoffs/reasonix/plan-eto-sentinel-v2.md        — 智子 v2 plan
docs/handoffs/reasonix/completion-eto-sentinel-v2.md   — Reasonix 完成回执
docs/handoffs/reasonix/plan-eto-sentinel-v2-fix.md     — 审计修复 plan
docs/handoffs/reasonix/plan-eto-mesh.md                — ETO Mesh plan（新增）
README.md                     — 智子 v2 教程 + 命令表更新
pyproject.toml                — v0.4.0 → v0.5.0
.pi/eto-sentinel.json         — 新配置格式（本地）
.pi/extensions/eto.ts         — 已删除（旧扩展文件）
```

---

## 五、环境快照

| 项目 | 值 |
|:-----|:----|
| ETO 版本 | v0.5.0 |
| 测试 | 17/17 PASS |
| 编排层代码量 | eto.ts ~740 行，Python stitches ~550 行（总计 < 1000 行编排约束 ✅） |
| 上次推送 | main + v0.5.0 tag → GitHub |
| 远程 | https://github.com/reoroy/evolutionary-teal-organization.git |
| 未推送改动 | 无（干净） |

### 记忆系统

| 位置 | 内容 |
|:-----|:------|
| `~/.eto/memory/guide.json` | 8 节使用指南（source+tags） |
| `~/.eto/memory/skills.jsonl` | 5 条种子经验 |
| `~/.eto/shared_memory/` | Agent 共享记忆（运行时写入） |
| `~/.pi/etoprofiles/profiles.json` | 3 个 Agent Profile |
| `~/.pi/eto-config.json` | 路由 + peer 配置 |
| `~/.pi/eto-sentinel.json` | 智子 v2 规则配置 |

### CLAUDE.md 记忆

```
memory/eto-sentinel-v2-usage.md     — 智子 v2 使用参考（本地 Claude Code）
memory/eto-feature-usage.md          — 全部功能使用方法（本地 Claude Code）
memory/eto-architecture-retrospective.md
memory/eto-project-state-handover.md
memory/eto-workflow.md
memory/eto-user-vision.md
...共 20+ 记忆文件
```

### 下次会话启动

```bash
# 读最新手账
ls -t docs/handover-*.md | head -1

# 读 ETO Mesh plan 开始实现
cat docs/handoffs/reasonix/plan-eto-mesh.md
```
